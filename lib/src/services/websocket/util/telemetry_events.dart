class TelemetryEvents {
  static String TWILSOCK_CONNECT =
      'twilsock.sdk.connect'; // establish WebSocket connection (with WebSocket handshake finished)
  static String TWILSOCK_INIT =
      'twilsock.sdk.init'; // send 'init' message and receive reply

}
